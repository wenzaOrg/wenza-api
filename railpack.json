{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "steps": {
    "extensions": {
      "commands": [
        {
          "cmd": "install-php-extensions intl zip gd bcmath exif"
        }
      ]
    }
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}